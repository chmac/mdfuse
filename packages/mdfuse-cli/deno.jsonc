{
  "tasks": {
    "dev": "deno run --unstable --allow-read --allow-env mod.ts",
    "compile": "deno compile --unstable --allow-read --allow-env --output build/mdfuse ./mod.ts"
  },
  "lock": true
}
